Run With venv Command:
```
sudo pip3 uninstall virtualenv
virtualenv --python=python3 venv
source venv/bin/activate
pip install -r requirements.txt
```

Run Without venv Command:
```
pip install -r requirements.txt
```